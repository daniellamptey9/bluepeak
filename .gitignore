# Minimal .gitignore for deployment
# We'll work on proper .gitignore later

# Only exclude these essential files for now
node_modules/
npm-debug.log
yarn-error.log

# Keep everything else for deployment